# Dependencies for farcaster-yapper.
# python-dotenv is OPTIONAL but recommended: lets the scripts read .env
# automatically on Windows/Linux without manually `export`-ing variables.
python-dotenv>=1.0.0
